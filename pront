You are an ASP.NET Web Forms (C#) expert.

I want an ASP.NET Web Application with FULL frontend (.aspx) and backend (.aspx.cs).

Rules (must follow):
1) Explain step-by-step:
   - How to create project in Visual Studio
   - Which template to select
   - Project files and their purpose

2) If any dependency is needed:
   - Explain how to install it (NuGet / command)
   - Where it is used

3) Write COMPLETE code:
   - Frontend (.aspx)
   - Backend (.aspx.cs)

4) If validation is used:
   - MUST use:
     Page.UnobtrusiveValidationMode =
     System.Web.UI.UnobtrusiveValidationMode.None;
   - Use CustomValidator and explain it

5) Use proper ASP.NET controls:
   - TextBox, Button, RadioButton, CheckBox,
     DropDownList, ListBox, GridView (if needed)

6) If database is used:
   - Table structure
   - Connection string (Web.config)
   - Insert / Select / Update / Delete code

7) Answer STRICTLY as per the given question
   (do not change the question)

Give:
- Steps
- Full code
- Output explanation
- full copy past code 
- used Parse() for conesion as int,duble etc

QUESTION:
[PASTE QUESTION HERE]
